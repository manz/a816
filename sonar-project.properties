sonar.projectKey=manz_a816
sonar.organization=manz

sonar.python.coverage.reportPaths=coverage.xml
sonar.python.version=3.10
sonar.sources=a816,script
sonar.tests=tests

# LSP server is glue code requiring full LSP integration tests (clients,
# stdio transport). Unit-tested via direct handler invocation; structural
# tests live in tests/test_lsp_*.py but full coverage needs end-to-end
# infrastructure that doesn't fit unit tests.
sonar.coverage.exclusions=a816/lsp/server.py
